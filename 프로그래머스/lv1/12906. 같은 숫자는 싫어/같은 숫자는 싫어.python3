def solution(arr):
    answer = []
    arr.append('*')
    
    for i in range(len(arr) - 1):
        if arr[i] == arr[i + 1]:
            continue
        else:
            answer.append(arr[i])
            
    return answer